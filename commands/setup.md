---
description: Build and configure the DynaMite memory system — binaries, server, MCP tools, and hooks
---

Run the setup skill: invoke `/dynamite-memory:setup` to build binaries, start the server, and activate MCP tools. Follow all steps in the `skills/setup/SKILL.md` file.
